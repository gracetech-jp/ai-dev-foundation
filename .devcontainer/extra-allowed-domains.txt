# 基盤リポジトリ固有の追加許可ドメイン（ADR-013 第1層）。
# init-firewall.sh の BASE_DOMAINS に足す形で読まれる。
#
# ここはビルド時にイメージへ焼かれる。実行時にワークスペースから読ませない——
# 読ませると、エージェントが行を足して sudo init-firewall.sh を叩くだけで
# 任意の宛先へ出られるようになり、第1層の意味が消える。
# 追加するときはこのファイルを編集して Rebuild する。
#
# 1行1ドメイン。# 以降はコメント。
#
# **現在、追加ドメインは1件も無い**（2026-08-04）。
# 唯一の登録だった通知サービスは、通知フックの廃止に伴って外した。
# bypassPermissions を既定にしたことで承認待ち通知が鳴る場面が消え、完了通知だけのために
# 外向きの宛先を1つ開け、トピック名を秘密として管理し続ける割に合わなくなったため。
#
# ファイル自体は残す。空でも init-firewall.sh は正常に動き（存在の有無しか見ない）、
# 消すと Dockerfile の COPY が壊れる。追加が必要になったらここへ1行足して Rebuild する。
