# Claude Code コマンドリファレンス

Claude Code で利用できるコマンドの一覧です。

---

## 組み込みスラッシュコマンド

チャット上で直接入力して使用します。

| コマンド | 概要 |
|---|---|
| `/help` | 利用可能なコマンドとヘルプを表示する |
| `/clear` | 現在の会話履歴をリセットする |
| `/compact` | 会話のコンテキストを圧縮してトークンを節約する |
| `/config` | モデル・テーマ等の設定メニューを開く |
| `/cost` | 現在セッションのトークン使用量とコストを表示する |
| `/doctor` | Claude Code のセットアップ状態を診断する |
| `/exit` / `/quit` | Claude Code を終了する |
| `/fast` | Fast モードのオン・オフを切り替える（Claude Opus による高速出力） |
| `/init` | カレントプロジェクトの `CLAUDE.md` を自動生成する |
| `/login` | Anthropic アカウントにサインインする |
| `/logout` | サインアウトする |
| `/memory` | メモリファイル（永続的な記憶）を編集する |
| `/model` | 使用するモデルを変更する |
| `/pr_comments` | GitHub PR のコメントを取得して表示する |
| `/release-notes` | Claude Code の最新リリースノートを表示する |
| `/review` | GitHub PR をレビューする |
| `/status` | アカウントおよびセッション情報を表示する |
| `/terminal-setup` | ターミナルの初期設定を行う |
| `/vim` | Vim キーバインドモードのオン・オフを切り替える |

---

## スキル（拡張スラッシュコマンド）

Claude Code に組み込まれた高機能コマンドです。

### 開発・品質

| コマンド | 概要 |
|---|---|
| `/code-review` | 現在の差分に対してコードレビューを実行する。`--comment` でPRコメント投稿、`--fix` で自動修正。`ultra` オプションでクラウドによる多重エージェントレビューも可能 |
| `/simplify` | 変更箇所を対象に、コードの簡略化・効率化・再利用性の改善を提案・適用する |
| `/security-review` | 現在のブランチの変更に対してセキュリティレビューを実施する |
| `/verify` | アプリを実際に起動し、変更が正しく動作しているかを確認する |
| `/run` | プロジェクトのアプリを起動して動作確認を行う |

### ドキュメント・レビュー

| コマンド | 概要 |
|---|---|
| `/init` | プロジェクトのコードベースを解析して `CLAUDE.md` を初期化・生成する |
| `/review` | GitHub PR をレビューする（差分レビューは `/code-review` を使用） |
| `/claude-api` | Claude API / Anthropic SDK のリファレンスを参照する（モデルID・料金・パラメータ等） |

### 設定・環境

| コマンド | 概要 |
|---|---|
| `/update-config` | `settings.json` を編集して Claude Code の動作を設定する。フック・権限・環境変数の管理に使用 |
| `/keybindings-help` | キーボードショートカットのカスタマイズを支援する |
| `/fewer-permission-prompts` | 過去のトランスクリプトを分析し、頻出の読み取り系コマンドを許可リストに追加してプロンプト数を削減する |

### 自動化・スケジューリング

| コマンド | 概要 |
|---|---|
| `/loop` | 指定したコマンドを一定間隔で繰り返し実行する（例: `/loop 5m /code-review`）。間隔省略でモデルが自動ペース調整 |
| `/schedule` | クラウド上でエージェントをスケジュール実行する定期タスクを作成・管理する |

---

## CLI オプション（ターミナル起動時）

ターミナルから `claude` コマンドを実行する際に使用できるオプションです。

| オプション | 概要 |
|---|---|
| `--model <モデルID>` | 使用するモデルを指定して起動する |
| `--print` / `-p` | 非インタラクティブモードで1回だけ実行して結果を出力する |
| `--output-format` | 出力形式を指定する（`text` / `json` / `stream-json`） |
| `--allowedTools` | 許可するツールを指定して起動する |
| `--dangerouslySkipPermissions` | 権限確認をスキップして全ツールを自動実行する（要注意） |
| `--resume <セッションID>` | 過去のセッションを再開する |
| `--continue` | 直前のセッションを継続する |
| `--add-dir <パス>` | 追加でアクセスを許可するディレクトリを指定する |

---

## モデル一覧

| モデルID | 特徴 |
|---|---|
| `claude-fable-5` | 最新モデル |
| `claude-opus-4-8` | 最高性能・複雑なタスク向け |
| `claude-sonnet-4-6` | バランス型（デフォルト） |
| `claude-haiku-4-5-20251001` | 高速・軽量タスク向け |

---

## 設定ファイル

| ファイルパス | 適用範囲 | 概要 |
|---|---|---|
| `~/.claude/settings.json` | グローバル（全プロジェクト共通） | デフォルトモデル・権限・フックなどを設定 |
| `.claude/settings.json` | プロジェクト単位 | プロジェクト固有の設定（チームで共有） |
| `.claude/settings.local.json` | プロジェクト単位（個人） | 個人用のローカル設定（`.gitignore` 推奨） |
| `CLAUDE.md` | プロジェクト単位 | AIへの指示・コーディングルールを記述 |
